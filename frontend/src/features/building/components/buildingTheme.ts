/**
 * 건물 화면에서만 쓰는 파란색.
 *
 * `styles/tokens.ts` 에는 "화면에 쓰는 값은 여기서만 정한다"고 적혀 있고 그게
 * 맞다. 그런데 앱의 강조색은 자주색(`color.accent`)인데 이 화면의 시안은
 * 파란색이라, 토큰에서 가져올 수 있는 값이 없다.
 *
 * 그래서 흩뿌리는 대신 여기 한곳에 모아 둔다. 팀이 이 파란색을 앱 전체에서
 * 쓰기로 하면 토큰으로 옮기고, 이 화면만의 예외로 남기기로 하면 여기 둔다.
 * 어느 쪽이든 값이 여러 파일로 번지지는 않는다.
 */
export const buildingColor = {
  /** 고른 층, 칩, 버튼 */
  accent: '#2a6bf2',
  /** 고른 층 판의 옆면. 강조색보다 어둡다. */
  accentEdge: '#1f4fd8',
  /**
   * 고른 층 판의 윗면.
   *
   * 더 연하게 하면 위아래 흰 판에 묻혀서 어느 층을 골랐는지 안 보인다.
   */
  accentSurface: '#cfe0ff',
  /** 고르지 않은 층 판의 윗면과 테두리 */
  plate: '#ffffff',
  plateLine: '#d3d7de',
  plateEdge: '#d8dbe2',
} as const;
